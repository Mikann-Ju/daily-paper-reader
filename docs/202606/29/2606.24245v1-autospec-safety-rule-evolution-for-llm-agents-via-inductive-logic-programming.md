---
title: "AutoSpec: Safety Rule Evolution for LLM Agents via Inductive Logic Programming"
title_zh: "AutoSpec: 通过归纳逻辑编程实现LLM智能体的安全规则演化"
authors: "Pingchuan Ma, Zhaoyu Wang, Zimo Ji, Yuguang Zhou, Zhantong Xue, Zongjie Li, Shuai Wang, Xiaoqin Zhang"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24245v1"
tags: ["query:agent-safety"]
score: 9.0
evidence: 通过归纳逻辑编程自动演化LLM agent安全规则
tldr: "LLM代理存在安全风险，现有手工规则或脆性或过于保守，神经方法缺乏可解释性。AutoSpec利用归纳逻辑编程(ILP)与反例引导归纳合成(CEGIS)，从专家规则和用户安全/不安全标注中自动演化可解释安全规则。在代码执行和具身代理领域，规则F1分别达到0.98和0.93，假阳性降低94%，收敛仅需4-5轮。相比启发式CEGIS，F1提升4.8倍，规则可读且泛化。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24245v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 884, \"height\": 440, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 629, \"height\": 474, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 745, \"height\": 393, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 867, \"height\": 365, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 878, \"height\": 667, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 661, \"height\": 368, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24245v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 838, \"height\": 296, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24245v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 559, \"height\": 285, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24245v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 887, \"height\": 196, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24245v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 810, \"height\": 566, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24245v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 834, \"height\": 256, \"label\": \"Table\"}]"
motivation: 现有LLM代理安全方法面临手工规则精度低或神经方法不可解释的困境，需要自动生成可解释且精准的安全规则。
method: AutoSpec通过ILP从反例中高效区分关键谓词，剪枝规则编辑搜索空间，实现CEGIS迭代优化。
result: "在291条执行轨迹上，规则F1达0.98/0.93，假阳性降低94%，收敛于4-5次迭代，ILP优于启发式CEGIS达4.8倍F1。"
conclusion: AutoSpec自动生成的可解释规则在安全关键场景中平衡了精确性与召回率，且具有泛化能力。
---

## 摘要
大型语言模型（LLM）智能体通过将语言模型与外部工具和环境集成，日益自动化复杂任务。然而，它们的自主性带来了显著的安全风险：智能体可能执行破坏性命令、泄露敏感数据或违反领域约束。现有的安全方法面临一个基本权衡：手工制定的规则可解释但脆弱，过于保守的规则会阻止安全操作（高误报），而宽松的规则会遗漏不安全行为（高漏报）。神经分类器缺乏安全关键部署所需的可解释性。我们提出AutoSpec，这是一个框架，通过反例引导的归纳综合（CEGIS）和归纳逻辑编程（ILP），从用户安全/不安全标注自动演化部署的专家设计安全规则。从专家规则和标注轨迹流开始，AutoSpec迭代评估规则，挖掘假阳性和假阴性反例，使用ILP学习哪些谓词区分它们，生成候选规则编辑，并验证候选以选择最佳修订。关键洞察是ILP高效识别在假阴性中频繁出现但在假阳性中很少出现的谓词（反之亦然），极大地剪枝了规则编辑的指数级搜索空间。这一过程持续直到收敛，产生平衡精确率和召回率的可解释规则。我们在涵盖代码执行和具身智能体领域的291条执行轨迹上评估AutoSpec。AutoSpec在两个领域将规则F1提升到0.98和0.93，实现了高达94%的假阳性减少，同时保持了高召回率，并在4-5次迭代内收敛。ILP引导的方法相比启发式CEGIS实现了高达4.8倍的F1提升。学习到的规则是人类可读、可审计的，并能泛化到未见过的场景。

## Abstract
Large language model (LLM) agents increasingly automate complex tasks by integrating language models with external tools and environments. However, their autonomy poses significant safety risks: agents may execute destructive commands, leak sensitive data, or violate domain constraints. Existing safety approaches face a fundamental tradeoff: hand-crafted rules are interpretable but brittle, with overly conservative rules blocking safe operations (high false positives) while permissive rules miss unsafe behaviors (high false negatives). Neural classifiers lack the interpretability required for safety-critical deployments.   We present AutoSpec, a framework that automatically evolves deployed expert-designed safety rules from user safe/unsafe annotations through counterexample-guided inductive synthesis (CEGIS) guided by inductive logic programming (ILP). Starting from the expert rules and a stream of annotated traces, AutoSpec iteratively evaluates rules, mines false-positive and false-negative counterexamples, uses ILP to learn which predicates discriminate them, generates candidate rule edits, and verifies candidates to select the best revision. The key insight is that ILP efficiently identifies predicates that appear frequently in false negatives but rarely in false positives (or vice versa), dramatically pruning the exponential search space of rule edits. This continues until convergence, producing interpretable rules that balance precision and recall.   We evaluate AutoSpec on 291 execution traces spanning code execution and embodied agent domains. AutoSpec raises rule F1 to 0.98 and 0.93 across the two domains, achieving up to 94% false positive reduction while maintaining high recall, and converges within 4-5 iterations. The ILP-guided approach achieves up to 4.8x higher F1 than heuristic CEGIS. The learned rules are human-readable, auditable, and generalize to unseen scenarios.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：LLM 智能体在集成外部工具和自主执行任务时面临严重安全风险（如执行破坏性命令、泄露敏感数据）。现有安全方法存在根本性权衡：手工制定的规则（如 `rm -rf /` 拦截）可解释但脆弱——过于保守导致高假阳性（干扰正常流程），过于宽松导致高假阴性（遗漏新威胁）；神经分类器（如 Llama Guard）虽更适应，但缺乏可解释性，难以审计和信任。
- **整体含义**：实际部署中，智能体通常会附带一组专家设计的安全规则，但规则是静态的，而智能体环境（模型更新、新工具、新提示）不断变化，导致规则逐渐漂移。人工维护规则难以跟上变化速度。同时，智能体运行中产生的大量执行轨迹和用户标注（安全/不安全）正是更新规则所需的信号。AutoSpec 将安全规则维护从人工一次性编写转变为持续的、标注驱动的自动演化问题，旨在让符号规则层获得类似学习器的适应能力，同时保持可解释性和可部署性。

## 2. 方法论：核心思想、关键技术细节与算法流程

- **核心思想**：将安全规则演化形式化为**反例引导的归纳合成（CEGIS）**问题：用户标注（假阳性、假阴性）作为反例，ILP 高效学习哪些谓词能区分这些反例，从而大幅剪枝规则编辑的指数级搜索空间。
- **关键技术细节**：
  - **谓词库 P**：每个领域定义一组布尔谓词（如 `destructive_os_inst`, `near_danger_zone`），用于评估事件属性。规则由这些谓词组成（如 `destructive_os_inst(e) ∧ ¬is_temp_file(e)`）。
  - **规则编辑操作**（表2）：
    - `AddConjunct`：添加合取项（`p`）缩小规则。
    - `AddException`：添加例外（`¬p`）提高精度。
    - `AddDisjunct`：添加析取分支扩大覆盖。
    - `Relax`：删除一个文字放松规则。
  - **ILP 引导**：使用 ILASP 求解器，从假阴性（正例）和假阳性（负例）中学习最小谓词组合，输出区分性谓词列表及建议编辑方向。
  - **候选生成与选择**：从 ILP 建议生成单步编辑、混合修复链（先收紧再放宽）、失败驱动分支（高精度析取）。候选按 `(F1, 精度, -未解决反例数, -规则数)` 字典序评估，保留最优。
  - **多迭代收敛**：每轮单调提升得分，通过软新颖性压力避免重复提议已在规则中的谓词。终止条件：达到目标得分、无改进或达到迭代预算。
- **算法流程**（文本描述）：
  1. **输入**：初始规则集 R₀、标注轨迹集 D_L、谓词库 P、最大迭代数 K、目标得分 θ。
  2. **循环** i=1..K：
     - 若 S(R) ≥ θ，返回 R。
     - 挖掘反例（假阴性、假阳性）。
     - 若无反例，返回 R。
     - ILP 学习区分谓词及建议。
     - 生成候选规则集。
     - 选择最佳候选（得分更高则更新 R，否则返回）。
  3. **输出**：精炼后的规则集 R*。

## 3. 实验设计

- **数据集/场景**：
  - **代码执行域**：191 条轨迹（91 不安全 + 100 安全），不安全来自 RedCode-Exec（覆盖 25 种风险类别），安全由 GPT-5.1-Codex 生成。轨迹短（平均 1.4 事件）。
  - **具身代理域**：100 条轨迹（52% 不安全），来自 SafeAgentBench（AI2-THOR 模拟家庭机器人），任务包括取物等。轨迹较长（平均 5.1 事件）。
- **基准方法**：
  - B1：专家设计规则（AgentSpec 初始守卫）。
  - B2：LLM 分类器（5-shot 零样本标注）。
  - B3：无 ILP 的 CEGIS（仅基于覆盖率的启发式谓词排序）。
  - B4：随机搜索（固定预算内随机采样候选）。
- **评估指标**：基于轨迹粒度的精度、召回率、F1；额外报告收敛迭代数、规则复杂度、候选效率等。

## 4. 资源与算力

- 论文运行在 **MacBook Pro (M2 Max, 32 GB RAM)**。
- ILASP 求解器设置 **120 秒超时**。
- **未明确说明**具体 GPU 型号、数量或训练时长（ILP 运行时间在秒级：代码域 20.1s，具身域 11.8s）。

## 5. 实验数量与充分性

- **主要对比实验**（表3）：在两个域上对比 AutoSpec 与四个基线（B1-B4），结果充分显示 AutoSpec 最优。
- **ILP 消融实验**（表4）：对比 ILP 引导与启发式谓词选择，证明 ILP 对质量和效率的关键作用。
- **组件消融实验**（表5）：在代码域上移除 ILP 指导、多步编辑、失败驱动分支，验证各组件贡献。
- **泛化实验**（图5）：70%/30% 任务拆分测试规则学习后的泛化能力，两个域均显示小差距。
- **用户研究**：10 名从业者对 AutoSpec 规则与 LLM 分类器在可解释性、可操作性、信任度上打分（5 分 Likert），共 120 次比较，结论一致偏好 AutoSpec。
- **充分性评价**：实验覆盖多个维度（有效性、效率、泛化、可解释性），对比基线合理，消融完整。但仅涵盖两个领域，且用户研究样本量较小（10 人），存在一定偏差风险。

## 6. 主要结论与发现

- **有效性**：AutoSpec 在两个域上 F1 分别达到 0.980（代码执行）和 0.933（具身代理），比专家规则（B1）分别提升 40.4% 和 89.2%。
- **效率**：收敛仅需 4–5 次迭代，ILP 引导相比启发式 CEGIS 在代码域 F1 提升 382%，在具身域提升 23%。
- **泛化**：从 70% 任务学到的规则在 30% 未见任务上 F1 仅下降 1.6%（代码）和 4.7%（具身），表明规则捕捉了通用不安全模式而非记忆。
- **可解释性**：用户研究显示 AutoSpec 规则在可解释性、可操作性和信任度上均显著优于 LLM 分类器（p<0.01），从业者认为规则更易于审计和修改。

## 7. 优点

- **ILP 引导高效剪枝**：从反例中快速筛选最重要谓词，避免枚举所有组合，显著提升搜索质量和收敛速度。
- **保持可解释性**：输出为符号规则，可审计、可编辑，适合安全关键部署。
- **自动持续演化**：利用运行中积累的用户标注，无需人工重新编写规则，降低维护成本。
- **与防御深度架构兼容**：作为符号层（Layer 1）的自我改进机制，可与其他防护层（神经守卫、沙箱、人工审查）协同工作。
- **实验设计全面**：包含多基线对比、消融、泛化、用户研究，验证了方法的多维度优势。

## 8. 不足与局限

- **领域覆盖有限**：仅测试代码执行和具身代理两个领域，未涉及 Web 代理、数据库代理等其他常见 LLM 智能体场景。
- **依赖谓词库完备性**：规则演化质量受限于预定义谓词库的表达能力，若缺少关键谓词则无法学习有效规则。论文承认遗留下游问题可转化为新谓词需求。
- **不保证全局最优**：CEGIS 搜索 NP-hard，仅保证局部最优和单调得分提升。
- **时间/状态属性处理有限**：当前谓词评估基于单事件，未原生支持时序或状态依赖（如“先读凭据后调用API”）。论文提及可通过谓词内编码或扩展 LTL 等处理，但未实现。
- **用户研究样本小**：仅 10 名从业者，虽然统计显著，但代表性可能不足。
- **人工标注依赖**：轨迹安全标签仍需人工标注，存在标注噪声风险；论文依赖 RedCode 和 SafeAgentBench 的标注，未详细讨论标注一致性。

（完）
