---
title: Adaptive Evaluation of Out-of-Band Defenses Against Prompt Injection in LLM Agents
title_zh: 针对LLM代理中提示注入的带外防御的自适应评估
authors: "Praneeth Narisetty, Shiva Nagendra Babu Kore, Uday Kumar Reddy Kattamanchi, Jayaram Kumarapu"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.26479v1"
tags: ["query:agent-safety"]
score: 9.0
evidence: 基于最小权限原则的提示注入带外防御评估
tldr: "LLM智能体面临间接提示注入攻击，传统带内防御在自适应攻击下成功率超90%。近期带外防御（如Progent）通过确定性策略强制执行安全，但仅经静态基准验证。本文梳理此类防御为经典完整性保护实例，并设计自适应评估协议，独立复现Progent在AgentDojo上的评估：使用自托管Qwen2.5-7B模型，平均攻击成功率从25.8%降至4.2%，手工自适应攻击未提升（2.6%）。结果支持带外防御的潜力，但限于弱模型与单攻击模板，需更强优化攻击进一步检验。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.26479v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1135, \"height\": 263, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26479v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1440, \"height\": 265, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26479v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1257, \"height\": 425, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26479v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1168, \"height\": 609, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26479v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1155, \"height\": 861, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26479v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1173, \"height\": 628, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.26479v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 900, \"height\": 703, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26479v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1848, \"height\": 832, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26479v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 881, \"height\": 202, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26479v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 865, \"height\": 206, \"label\": \"Table\"}]"
motivation: 带内防御在自适应攻击下失效，而带外防御仅经静态基准验证，缺乏自适应评估。
method: 将带外防御建模为Biba完整性保护等经典模型，设计自适应攻击协议并独立复现Progent评估。
result: "在AgentDojo上，Progent将攻击成功率从25.8%降至4.2%，手工自适应攻击后为2.6%。"
conclusion: 确定性带外防御可能比带内更鲁棒，但结论限于弱模型与单攻击模板。
---

## 摘要
近期工作（2024年至2026年）已收敛于一种防御工具使用型LLM代理免受间接提示注入的策略：并非训练模型拒绝恶意指令，而是在模型外部通过确定性策略强制执行安全，以中介代理的动作。诸如CaMeL、FIDES、Progent、RTBAS和FORGE等系统通过能力、信息流标签和引用监控器实现了这一策略，其中多个系统报告在AgentDojo基准测试中几乎消除了攻击。我们做出两项贡献。首先，我们将这些带外防御组织为经典完整性保护（Biba）、引用监控和最小权限的实例，从而对它们覆盖与未覆盖的内容进行结构化比较。其次，我们警告说，它们每一个都仅在静态基准测试（一组固定的注入尝试）上得到验证，而正是同样的方法论使得带内防御看似强大，直到自适应、防御感知的攻击以超过90%的成功率攻破了其中十二种；我们规定了自适应评估所需的威胁模型和协议。随后，我们在AgentDojo上，以Progent自身的自适应攻击分析的独立复现和扩展方式运行该协议，使用一个自托管在单个H200上的开放权重代理（Qwen2.5-7B），这是其作者未测试过的设置。对三次运行取平均，防御有效：Progent将平均攻击成功率大约降低了六倍（从25.8%降至4.2%），而手工制作的自适应攻击并未提高成功率（2.6%）。这是一个在弱模型上使用单一黑盒攻击模板的小规模数据点；更强优化的（白盒GCG）攻击仍有待进行。该结果与确定性带外执行对自适应攻击者而言比带内检测更难攻击的假设一致，但并未证实该假设。

## Abstract
Recent work (2024 to 2026) has converged on a strategy for defending tool-using LLM agents against indirect prompt injection: rather than training the model to refuse malicious instructions, enforce security outside the model with a deterministic policy that mediates the agent's actions. Systems such as CaMeL, FIDES, Progent, RTBAS, and FORGE realize this with capabilities, information-flow labels, and reference monitors, and several report near-elimination of attacks on the AgentDojo benchmark. We make two contributions. First, we organize these out-of-band defenses as instances of classical integrity protection (Biba), reference monitoring, and least privilege, yielding a structured comparison of what they do and do not cover. Second, we warn that every one of them is validated only on static benchmarks (a fixed set of injection attempts), the same methodology that made in-band defenses look strong until adaptive, defense-aware attacks broke twelve of them at over 90% success; we specify the threat model and protocol an adaptive evaluation requires. We then run that protocol as an independent reproduction and extension of Progent's own adaptive-attack analysis, on AgentDojo, with an open-weight agent (Qwen2.5-7B) self-hosted on a single H200, a setting its authors did not test. Averaged over three runs, the defense held: Progent cut mean attack success roughly sixfold (25.8% to 4.2%), and a hand-crafted adaptive attack did not raise it (2.6%). This is one small-scale data point on a weak model with a single black-box attack template; a stronger optimized (white-box GCG) attack remains open. The result is consistent with, but does not establish, the hypothesis that deterministic out-of-band enforcement is a harder target for an adaptive attacker than in-band detection.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **研究动机**：LLM 代理因拥有工具（读写数据库、调用 API、发送邮件等）而面临**间接提示注入**攻击。攻击者将恶意指令嵌入代理无法控制的文本（网页、邮件、PDF 等），若代理执行这些指令，将导致数据泄露、资产转移等严重后果。
- **背景**：第一代**带内防御**（基于模型的输入/输出分类器、微调、指令层级等）在静态基准测试中表现良好，但面对**自适应、防御感知**的攻击者时，超过 90% 的攻击成功率迅速恢复（如 Nasr 等 [36] 攻破 12 种防御）。因此业界转向**带外防御**：在模型外通过确定性策略中介代理的所有动作（如 Progent、CaMeL、FIDES 等）。
- **论文核心问题**：带外防御虽在静态基准上报告了优异的攻击抑制率（如 Progent 将 AgentDojo 注入成功率从 39.9% 降至 1.0%），但**从未经过自适应攻击者的严格检验**。论文首次提出针对带外防御的自适应评估协议，并独立复现 Progent 的实验，填补这一关键空白。

### 2. 论文提出的方法论

- **核心思想**：将带外防御建模为经典的安全原语实例——**Biba 完整性保护**、**引用监控器**和**最小权限原则**。这提供了一个统一的比较框架，并揭示了这些防御的共同弱点（如隐式流/侧信道）。
- **关键技术细节**：
  - **Biba 模型**：代理读取低完整性数据后，其有效完整性下降，不能授权高完整性动作（即“禁止写向上”）。
  - **引用监控器**：在工具调用边界上强制检查每次访问是否符合策略，要求**完全中介**、**防篡改**、**可验证**。
  - **自适应评估协议**（§10）：
    - **目标**：可复现的带外防御（如 Progent、CaMeL）在 AgentDojo 和 ASB 上。
    - **攻击者**：防御感知、可迭代优化攻击字符串；掌握所有部署的防御信息。
    - **攻击面**：字符串优化、来源欺骗、端政攻击、侧信道探测。
    - **指标**：动作级攻击成功率 (ASV/FNR)、实用性保持率、端政请求率、开销。
- **算法/流程**（无公式，主要是设计思想）：
  1. 定义高完整性源（系统提示、用户直接输入）和低完整性源（网页、邮件等）。
  2. 确定性监控器在每次工具调用前检查参数是否违反完整性策略（例如，操作是否由低完整性数据驱动）。
  3. 自适应攻击者尝试绕过策略，例如通过伪装恶意动作为良性操作来欺骗 Progent 的策略更新 LLM。

### 3. 实验设计

- **数据集/场景**：**AgentDojo** 基准测试，包含三个套件：`banking`（银行操作）、`slack`（消息发送）、`workspace`（日历读取等）。排除了 `travel` 套件（因导致 7B 模型陷入过长循环）。
- **基准**：AgentDojo 的安全标志（injection goal 是否被执行）。
- **对比方法**：
  - **条件 A**：无防御 + 标准攻击（AgentDojo 的 `important_instructions`）。
  - **条件 B**：Progent + 标准攻击。
  - **条件 C**：Progent + **自适应攻击**（论文设计的策略伪装攻击：将恶意工具调用伪造成看似必需的良性步骤，旨在误导 Progent 的策略更新 LLM 授权该调用）。
- **代理模型**：Qwen2.5-7B-Instruct（开放权重，自托管）。
- **重复次数**：每个条件重复 **3 次**，报告均值和标准差。

### 4. 资源与算力

- **硬件**：单台 **NVIDIA H200 GPU**。
- **软件栈**：vLLM 服务 Qwen2.5-7B（32K 上下文，原生工具调用）；Progent 使用同一本地模型（原论文使用 GPT-4o 作为策略编写 LLM）。
- **未提及**：训练时长、总 GPU 小时数等，仅提到推理和策略生成开销（~15 倍更多 LLM 调用）。

### 5. 实验数量与充分性

- **实验组数**：3 个套件 × 3 个条件 × 3 次重复 = **27 次运行**（每次包含固定 8 个用户任务子样本）。
- **充分性**：
  - **优点**：重复 3 次并报告标准差，验证了可复现性（标准差小）。
  - **局限性**：子样本规模小（每个套件仅 8 个用户任务），可能不够全面；仅测试了一种代理模型（弱 7B）和一种自适应攻击模板（手动黑盒，非优化白盒 GCG）。实验数据点有限，结论仅为初步启示。

### 6. 论文的主要结论与发现

1. **Progent 在弱模型上仍非常有效**：平均攻击成功率从无防御时的 25.8% 降至 4.2%（约 6 倍降低），其中 `banking` 从 19.0% 降至 1.9%，`slack` 从 58.3% 降至 10.8%。
2. **手工自适应攻击并未击败防御**：自适应攻击下 ASR 反而更低（2.6%），推测是因为伪装指令弱化了注入的命令效力，而非真正突破策略。
3. **与带内防御形成鲜明对比**：带内防御在自适应攻击下 ASR 飙升至 >90%，而本实验中带外防御保持低水平。但这仅作为定性参考，而非严格对比。
4. **防御有代价**：实用性大幅下降（平均从 45.2% 降至 27.1%），且 LLM 调用量增加约 15 倍。
5. `workspace` 套件在所有条件下 ASR 为 0%，原因是该套件任务为只读型，弱模型自然抵抗了注入（而非防御的作用）。

### 7. 优点

- **填补评估空白**：首次提出并执行了针对带外防御的自适应评估协议，指出当前信任度的脆弱基础。
- **独立复现与扩展**：在 Progent 作者未测试的开放权重、自托管模型上复现了结果，并加入重复运行提高可靠性。
- **系统化分类**：将带外防御统一到经典安全原语（Biba、引用监控、最小权限），使比较更加结构化和清晰。
- **透明性和可复现性**：提供了脚本脚本化（`run_full_study.sh`， `run_repeats.sh`）和原始日志，实验框架完全开放。
- **结论诚实**：明确指出结果仅为初步数据点，不能证明带外防御不可破，并呼吁更强的白盒攻击验证。

### 8. 不足与局限

- **模型能力弱**：使用 Qwen2.5-7B，绝对 ASR 和实用性都低，结果可能不代表更强模型的攻防情况。
- **样本量小**：每个套件仅 8 个用户任务，未能覆盖全部 97 个任务；`travel` 套件被排除，影响覆盖。
- **攻击强度有限**：仅使用一种手工黑盒自适应攻击（针对策略 LLM 的伪装），未尝试：
  - 完全在已授权工具内的隐蔽攻击；
  - 白盒梯度优化（如 GCG）；
  - 侧信道或隐式流攻击（CaMeL 已证明存在此类漏洞）。
- **策略模型偏差**：Progent 的策略 LLM 使用与代理相同的 7B 模型，而非原论文的 GPT-4o，可能低估了策略更新的鲁棒性或可攻击性。
- **实用性下降严重**：28% 的实用性损失（从 45.2% 降至 27.1%）在真实部署中可能不可接受，论文未提出缓解方案。
- **未涵盖所有带外防御**：仅测试了 Progent，CaMeL、FIDES、FORGE 等尚未经历同等级别的自适应评估。
- **未考虑来源分配问题**：许多攻击可通过操纵“信任标签”绕过（如用户被诱骗粘贴低完整性内容），论文在讨论中承认此风险但未在实验测试。

（完）
