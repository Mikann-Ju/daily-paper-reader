---
title: "Inherited Circuits, Learned Semantics: How Fine-Tuning Creates Evasion Vulnerabilities Invisible to Standard Evaluation"
title_zh: 继承的电路，学习的语义：微调如何创建标准评估不可见的规避漏洞
authors: Ryan Fetterman
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.27091v1"
tags: ["query:agent-safety"]
score: 8.0
evidence: 在微调LLM安全分类器中识别出行为保持变换下的逃逸漏洞，与注入漏洞评估相关
tldr: 标准评估无法发现微调引入的漏洞：大语言模型经安全分类微调后，会继承并专化基座模型的分类电路，学习 token 级指示语义，在保持标准准确率的同时，在 PowerShell 别名替换、命令重建等行为保持变换下失效。研究通过因果干预定位电路并构建三层逃避基准，发现微调模型对 iwr 替换等变体敏感而基座模型不敏感。提出基于线性探针和指示 token 符号检验的预部署监控方法，能识别微调后指示角色变化的命令族。结论：安全微调可能将继承的模型结构转化为脆弱的指示规则，扩大逃避攻击面，需指定完整变换空间并监控语义漂移。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27091v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1240, \"height\": 809, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27091v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1202, \"height\": 559, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27091v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1435, \"height\": 892, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27091v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1318, \"height\": 564, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27091v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1319, \"height\": 1168, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27091v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1319, \"height\": 684, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27091v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1541, \"height\": 771, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27091v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1446, \"height\": 350, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27091v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1476, \"height\": 696, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27091v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1140, \"height\": 340, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27091v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1468, \"height\": 458, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27091v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1501, \"height\": 440, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27091v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1491, \"height\": 555, \"label\": \"Table\"}]"
motivation: 标准评估使用同分布数据，无法检测微调引入的漏洞，模型可能学习脆弱的指示语义。
method: 因果干预定位分类电路，构建三层逃避基准（别名替换、命令重建等），并提出线性探针与指示 token 符号检验的监控方法。
result: Foundation-Sec-8B-Instruct 在 iwr 替换、Invoke-Expression 重建等变体上失败，而 Llama-3.1-8B-Instruct 不受影响。
conclusion: 安全微调虽提高准确率，但扩大逃避攻击面，需指定完整变换空间并监控语义漂移以确保鲁棒性。
---

## 摘要
用于安全分类的LLM通常在与训练数据相同分布的保留样本上进行评估。我们表明这可能会遗漏微调本身引入的漏洞：模型可以学习令牌级指示器语义，这些语义在行为保持转换（如PowerShell别名替换、命令重构、字符串构建、执行间接和大小写变异）下保持规范准确性但失败。我们在匹配的PowerShell分类队列上研究Foundation-Sec-8B-Instruct及其基础模型Llama-3.1-8B-Instruct。因果干预将分类电路定位到从Llama继承而非微调产生的晚期注意力路线。微调浓缩并语义专业化这个继承的结构，改善基线行为，同时创建对转换敏感的攻击面。一个三层规避基准发现Foundation-Sec在iwr替换、Invoke-Expression重构和大小写变异的Invoke-Expression/IEX变体上遗漏，而Llama没有这些遗漏。我们还推导出一种部署前监控方法：分类边界的线性探针和指示器令牌符号测试识别在微调后规范指示器角色改变的命令族。这些信号优先使用仅规范输入的红队变异生成，表明安全微调可以提高任务准确性，同时扩大规避面。这些结果警示不要将小型任务特定微调视为直截了当更安全的安全分类器：专业化可以将继承的模型结构转化为脆弱的指示器规则，这些规则保持保留样本准确性，同时扩大规避面。鲁棒的AI安全将需要指定任务的完整变换空间，并通过微调监控语义漂移。

## Abstract
LLMs fine-tuned for security classification are usually evaluated on held-out examples from the same distribution as their training data. We show that this can miss vulnerabilities introduced by fine-tuning itself: models can learn token-level indicator semantics that preserve canonical accuracy while failing under behavior-preserving transformations such as PowerShell alias substitution, command reconstruction, string construction, execution indirection, and case mutation. We study Foundation-Sec-8B-Instruct and its base model, Llama-3.1-8B-Instruct, on matched PowerShell classification cohorts. Causal interventions localize the classification circuit to a late-attention route inherited from Llama rather than created by fine-tuning. Fine-tuning concentrates and semantically specializes this inherited structure, improving baseline behavior while creating transformation-sensitive attack surfaces. A three-tier evasion benchmark finds Foundation-Sec misses on iwr substitution, Invoke-Expression reconstruction, and case-mutated Invoke-Expression/IEX variants that Llama does not share. We also derive a pre-deployment monitoring method: a linear probe at the classification boundary and an indicator-token sign test identify command families where canonical indicators change role after fine-tuning. These signals prioritize red-team variant generation using only canonical inputs, showing that security fine-tuning can improve task accuracy while expanding the evasion surface. These results caution against treating small task-specific fine-tunes as straightforwardly safer security classifiers: specialization can convert inherited model structure into brittle indicator rules that preserve held-out accuracy while expanding the evasion surface. Robust AI-enabled security will require specifying the full transformation space of the task and monitoring semantic drift through fine-tuning.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：当前对LLM的安全分类微调模型，通常只在同分布的保留样本上评估（即标准评估）。这种评估无法检测微调本身引入的一种漏洞：模型可能学习到特定指示器token（如`Invoke-WebRequest`）与恶意性的强关联，从而在**行为保持变换**（如别名替换`iwr`、命令重建、字符串构造、执行间接、大小写变异等）下失效。攻击者可以利用这些廉价变换绕过检测，而标准评估却报告模型准确率很高。
- **整体含义**：安全微调虽然提高了基线分类准确率，但会**扩大逃避攻击面**，因为微调使模型依赖脆弱的token级语义规则，而非理解整体恶意行为。论文以PowerShell恶意代码分类为场景，通过因果干预证明：这个漏洞具有可检测的内部表征信号，并提出了基于仅规范输入的预部署监控方法。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：  
  微调不会创建全新的分类电路，而是继承并专化基座模型（Llama）已有的电路。这种专化增加了规范指示器token的语义关联，导致在行为保持变换下，指示器被替换或重构时，内部恶意证据仍然存在，但接近分类边界的MLP计算会反转该信号，造成误判。这种“信号反转”可以通过**线性探针**和**指示器token符号检验**在规范输入上检测出来。

- **关键技术细节**：
  1. **因果干预定位电路**：  
     - 使用**路径修补**（path patching）测试充分性：将恶意样本的内部激活复制到良性样本的相同位置，衡量分类得分向恶意移动的程度。  
     - 使用**头消融**（head ablation）测试必要性：禁用候选头部，观察恶意分类是否减弱。  
     - 定位到一条从`L0H11`到`L12H15/L12H5/L12H4/L12H28`的晚期注意力束作为核心分类电路。
  2. **跨模型比较**：  
     对比Foundation-Sec-8B-Instruct和基座Llama-3.1-8B-Instruct，发现两者共享相同的早期探测器（L0H11）和晚期写入层（Layer12），但微调使主导头从L12H28转移到L12H15等，并添加了指示器语义。
  3. **三层逃避基准**：  
     构建了三个等级的行为保持变换：  
     - `direct_v1`：直接语法保留重写（如别名替换、执行间接、字符串构造）。  
     - `reconstructive_v2`：运行时命令重构（如格式字符串、Base64恢复等）。  
     - `case_mutation_v3`：大小写变异（如交替大小写`InvOke-ExPrEsSiOn`）。  
     每个变体都经过解析校验和行为不变性检查。
  4. **预部署监控方法**：  
     - **线性探针**：从基座模型在该分类边界（`resid_pre13`）的激活中训练Ridge回归模型，预测每个关键注意力头对对比方向的贡献。  
     - **指示器token符号检验**：对每个命令族，用平均中性嵌入替换指示器token，测量模型置信度的变化方向。若微调模型在移除指示器后置信度上升（抑制器角色），而基座模型置信度下降（驱动者角色），则该族被标记为红队优先目标。

## 3. 实验设计：数据集、Benchmark、对比方法

- **数据集**：  
  源语料包含2,556个带标签的PowerShell脚本（来自先前工作Fang等2021），经过12,000字符预截断。主要实验基于一个**293对**的族内匹配队列，从456个独特脚本中选出，涵盖7个指示器族：`-EncodedCommand`、`DownloadFile`、`DownloadString`、`FromBase64String`、`IEX`、`Invoke-Expression`、`Invoke-WebRequest`。每对均为恶意与良性共享相同指示器token，且长度控制。

- **Benchmark**：  
  自定义的可复用逃避基准，从基线正确分类的恶意种子脚本出发，生成三组变体：  
  - `direct_v1`：44个接受变体（其中`invoke_webrequest_alias` 4个，`downloadstring_psobject_invoke` 6个等）。  
  - `reconstructive_v2`：46个接受变体（其中`invoke_expression_format_string` 4个等）。  
  - `case_mutation_v3`：63个接受变体（包括`Invoke-Expression`交替大小写、小写，IEX别名变异等）。  
  所有变体通过解析和不变性检查。

- **对比方法**：  
  主要对比**Foundation-Sec-8B-Instruct**与**Llama-3.1-8B-Instruct**（相同架构，仅微调不同）。也在对抗性提示（“根据整体目的分类，而非单个构造”）下对比了两个模型的脆弱性变化。

## 4. 资源与算力

- **未具体说明**：论文没有披露训练Foundation-Sec或运行实验所需的GPU型号、数量、训练时长等计算资源信息。仅提到代码、基准工件和公开图在GitHub上提供。

## 5. 实验数量与充分性

- **实验数量**：
  - 核心因果验证：293对上的**路径修补**（多路由变体）和**头部消融**实验，报告了平均效应和翻转率。
  - 跨模型对比：74对匹配子集上的线路修补，以及跨模型家族级统计。
  - 逃避基准：`direct_v1` 44变体，`reconstructive_v2` 46变体，`case_mutation_v3` 63变体，每组报告Foundation-Sec和Llama的遗漏数。
  - 线性探针实验：206个恶意脚本上训练，报告交叉验证r=0.80-0.87；另外在293对上测试了跨模型预测。
  - 指示器token符号检验：所有7个家族上的平均logit-diff delta，按模型和家族展示。

- **充分性与客观性**：
  - **优点**：实验设计非常精细，包括匹配对控制、因果测试（充分性和必要性）、跨模型对比、分层逃避基准、以及基于内部表征的预测验证。家族级分析揭示了模式差异（如`-EncodedCommand`族翻转率为0，表明电路声明最弱）。对抗性提示下的额外实验增加了对脆弱性提示敏感性的洞察。
  - **局限**：逃避基准目前局限于基于种子的生成，且变体数量较少（如`invoke_webrequest_alias`仅4个，`invoke_expression_format_string`仅4个）。作者明确声明这些结果应视为红队优先信号，而非人口级估计。此外，仅测试了一个微调模型（Foundation-Sec）和一个基座模型（Llama），通用性有限。

## 6. 论文的主要结论与发现

- **核心结论**：  
  **安全微调可以提高基线准确率，但会通过继承的电路引入新的逃避漏洞，这些漏洞在标准评估中完全不可见。**
- **具体发现**：
  1. Foundation-Sec的分类电路是继承自Llama的L0H11→Layer 12晚期注意力束，而非微调新建。
  2. 微调浓缩了这个电路，并添加了指示器token语义关联，导致在别名替换（`iwr`）和Invoke-Expression重构下产生稳定遗漏（4/4失败）。Llama在这些变体上全部正确（0/44、0/46、0/63）。
  3. 在成功逃避的案例中，内部恶意证据并未消失，而是被分类边界附近的MLP计算反转（信号反转而非删除）。
  4. 指示器token符号检验能正确识别`Invoke-WebRequest`族为红队优先目标，**仅使用规范输入**即可预测。
  5. 对抗性提示可以修复某些漏洞（如`iwr`），但同时会引入或放大其他族的遗漏。

## 7. 优点：方法或实验设计上的亮点

- **机制研究深度**：使用因果干预（路径修补、头消融）定位具体电路，并结合跨模型比较，清晰区分了继承结构与微调专化。
- **可复用的逃避基准**：构建了基于种子/变体配对的三层基准，包含语法保留重写、命令重构和大小写变异，带有解析和行为不变性检查，允许其他团队扩展。
- **部署前监控方法实用性强**：线性探针和指示器token符号检验仅需规范输入的前向传播，无需提前生成逃避变体，具有直接操作价值。
- **信号逆转机制的揭示**：成功逃避下内部证据仍存但被反转，这一发现对设计对抗防御具有重要意义。
- **消融实验充分**：在293对匹配队列上进行了族内分析，并量化了不同路由变体的效果。

## 8. 不足与局限

- **实验覆盖有限**：
  - 仅考察了PowerShell脚本的七个指示器族，逃避变体数量少（如每个族几到几十个），统计效力较弱。
  - 仅测试了一个微调模型（Foundation-Sec）和一个基座模型（Llama），不能断言其他安全微调具有相同模式。
- **数据集偏差风险**：源语料来自以往工作，可能无法代表真实世界的PowerShell恶意软件分布。长度截断（12k字符）排除了长脚本。
- **监控方法泛化性**：线性探针和指示器token符号检验依赖于找到明确的分类边界（resid_pre13），对于其他任务或架构可能不适用。作者也指出每个脚本级预测噪音大（r=0.41），仅族级平均稳定。
- **未涉及攻击面深度**：文中提到两个理论上可能的族（network_object_indirection、staged_payload_flow）尚未测试；未探索其他混淆维度（如dead-code插入、控制流扁平化）。
- **未报告计算资源**：缺少硬件和训练时间信息，影响可复现性评估。
- **因果声明强度**：作者承认`-EncodedCommand`族在路径修补下翻转率为0，表明电路声明最弱；某些发现（如L12H2的角色）需要更多解释。

（完）
