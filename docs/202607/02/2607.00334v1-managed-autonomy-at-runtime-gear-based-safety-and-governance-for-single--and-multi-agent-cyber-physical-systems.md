---
title: "Managed Autonomy at Runtime: Gear-Based Safety and Governance for Single- and Multi-Agent Cyber-Physical Systems"
title_zh: 运行时管理自治：基于档位的单智能体与多智能体信息物理系统安全与治理
authors: "Srini Ramaswamy, Wang Miaosheng"
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.00334v1"
tags: ["query:agent-safety"]
score: 9.0
evidence: 单/多代理信息物理系统的运行时安全治理
tldr: "自主智能体在无持续人类监督时面临安全违例、行为不稳定和连续性损失等失效模式。本文提出一种基于五个执行档位的离散时间控制系统，结合效用门控调度与事件驱动回退，并证明单智能体稳定性。多智能体场景中应用smart生命周期与共识门控，提供分布式安全保证。在三个UR5机器人装配单元上评估，异常检测率达99.6%，检测延迟降低3.5倍，并给出形式化安全证书。该方法将执行档位作为微观权限，分离动作控制与自治治理，显著提升运行时安全性。"
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.00334v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 833, \"height\": 364, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00334v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1218, \"height\": 780, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00334v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 772, \"height\": 406, \"label\": \"Table\"}]"
motivation: 解决自主智能体无持续人类监督时安全违例、行为不稳定和连续性损失等常见失效模式。
method: 提出离散时间控制系统，包含五个执行档位与效用门控调度，单智能体证明稳定性，多智能体结合smart生命周期和共识门控。
result: "在三台UR5机器人装配单元上异常检测率99.6%，延迟降低3.5倍，实现零碰撞保证。"
conclusion: 执行档位作为微观权限分离动作控制与自治治理，提升运行时安全性和稳定性。
---

## 摘要
自主智能体，无论是大语言模型驱动的软件智能体还是机器人物理智能体，在无持续人类监督下运行时面临一类常见的故障模式：未经验证的动作导致的安全违规、不受约束的循环导致的行为不稳定，以及未处理错误状态导致的连续性丢失。我们开发了\system{}，一种离散时间控制系统，它结合了五个执行档位（\Gobs{}、\Gsug{}、\Gplan{}、\Gexec{}、\Gint{}）与效用门控调度和事件驱动回退。对于单智能体场景，我们证明了单调稳定性、执行安全性、最终稳定化、回退完备性，以及等价于档位约束马尔可夫决策过程。对于多智能体信息物理系统（CPS），我们应用已建立的\smart{}管理自治生命周期，并将运行时证据映射到其四个治理状态（\Stable{}、\Meta{}、\Assisted{}、\Regulated{}）。共识门控、群体级李雅普诺夫分析、每个智能体的档位权限和会合控制提供了分布式安全性和稳定性保证，包括在所述假设下的零碰撞。我们在一个三智能体UR5机器人装配单元上评估了所得运行时，使用从NIST \emph{机器人手臂位置精度退化测量}数据集校准的故障幅度，进行了10,000次蒙特卡洛模拟。它实现了99.6%的异常检测率（单智能体基线为2.1%），将检测延迟降低了3.5倍，并提供了形式化的物理工作空间安全证书。执行档位充当\smart{}运行时治理状态下的微权限，将动作控制与自治治理分离。

## Abstract
Autonomous agents, whether LLM-driven software agents or robotic physical agents, face a common class of failure modes when operating without continuous human oversight: safety violations from unverified actions, behavioral instability from unconstrained loops, and continuity loss from unhandled error states. We develop \system{}, a discrete-time control system that combines five execution gears (\Gobs{}, \Gsug{}, \Gplan{}, \Gexec{}, \Gint{}) with utility-gated dispatch and event-driven fallback. For the single-agent case, we prove monotonic stability, execution safety, eventual stabilization, fallback completeness, and equivalence to a gear-constrained Markov decision process. For multi-agent cyber-physical systems (CPS), we apply the established \smart{} managed-autonomy lifecycle and map runtime evidence into its four governance states (\Stable{}/\Meta{}/\Assisted{}/\Regulated{}). Consensus gating, swarm-level Lyapunov analysis, per-agent gear authority, and rendezvous control provide distributed safety and stability guarantees, including zero collision under the stated assumptions. We evaluate the resulting runtime on a three-agent UR5 robotic assembly cell using fault magnitudes calibrated from the NIST \emph{Degradation Measurement of Robot Arm Position Accuracy} dataset across 10,000 Monte Carlo episodes. It achieves a 99.6\% anomaly detection rate versus 2.1\% for the single-agent baseline, reduces detection latency by $3.5\times$, and supplies a formal physical-workspace safety certificate. The execution gears act as micro-level permissions beneath the \smart{} runtime governance states, separating action control from autonomy governance.

---

## 论文详细总结（自动生成）

好的，这是根据您提供的论文内容，以中文、Markdown格式生成的结构化、深入且客观的总结。

### 论文核心分析：EntropyRuntime —— 基于档位的运行时自治管理

#### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：自主智能体（无论是LLM驱动的软件Agent还是物理机器人）在缺乏持续人类监督时，面临着三大相互关联的失效模式：
    - **安全违例**：执行未经验证的、可能造成不可逆后果的动作。
    - **行为不稳定**：在不同策略间反复振荡，或陷入退化循环，无法收敛。
    - **连续性丢失**：因未处理的错误状态而意外中止，丢失上下文，需要昂贵的重启。
    - **（多智能体场景）协调盲视**：单一Agent的传感器故障在没有被检测到时，可能危及其邻近Agent的安全。
- **整体含义**：当前智能体的自治性通常是“全有或全无”的、静态赋予的，缺乏一个根据运行时观测到的安全信号动态调整其行动权限的机制。论文旨在解决这一问题，通过构建一个形式化的运行时控制系统，实现对Agent行动的精细化管理，从而在保障安全与稳定性的前提下，最大化其自主能力。

#### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：论文提出了 **EntropyRuntime**，一个介于LLM推理和动作执行之间的离散时间控制系统，通过“档位”机制来控制Agent的“行动权限”，分离了**行动控制**与**自治治理**。

- **关键技术细节**：
    1.  **五档位状态机**：
        - 定义了五个递进的档位（Gears），每个档位限制了可执行动作的空间（A0 ⊂ A1 ⊂ ... ⊂ A4）。
        - `G0 OBSERVE`：只允许观察或安全保持。
        - `G1 SUGGEST`：允许生成计划，但无外部副作用。
        - `G2 PLAN`：允许执行受限的、可逆的或安全恢复性动作（如减速、查询信息）。
        - `G3 EXECUTE`：允许执行具有副作用的独立动作。
        - `G4 INTEGRATE`：允许在系统级宏状态（Stable）下进行集成的系统级协调。
        - **档位切换规则**：根据累积的**不稳定性度量** `σt` 和**错误标志** `ϵt` 进行确定性切换：稳定性提升则升档（Escalation），不稳定性或错误发生则降档（De-escalation）。

    2.  **效用门控调度**：
        - 定义一个效用函数 `U(s, a)`，评估状态-动作对的价值，包含任务进度、安全性和成本项。
        - 引入一个**效用门**，只有当 `U(s, a) ≥ θ`（安全阈值）时，动作才会被放行执行。这确保了只有“安全”的动作才能被执行。

    3.  **事件驱动回退**：
        - 当动作被效用门拒绝时，触发回退机制：记录拒绝、增加不稳定性 `σt`、尝试生成替代动作。若连续失败，则强制降档，甚至最终进入OBSERVE状态等待人工干预。

    4.  **单智能体形式化保证**：
        - **定理1 (单调稳定性)**：预期的`σt`是单调不增的。
        - **定理2 (执行安全)**：只有效用非负的动作才会被执行。
        - **定理3 (最终稳定)**：在平稳环境下，系统几乎必然会在有限时间内稳定在一个固定档位。
        - **定理4 (回退完备性)**：任何可恢复的错误状态都存在一条恢复路径。
        - **定理5 (表示定理)**：单智能体EntropyRuntime的策略集等价于一个档位约束的马氏决策过程（MDP）的策略集。

    5.  **多智能体（SMARt框架）扩展**：
        - 引入**共识效用门**：要求所有Agent的本地效用均 ≥ θ 时，才允许执行联合动作。
        - 引入**群体李雅普诺夫函数** `Vswarm`：基于实际/感知位置误差和传感器漂移来量化物理工作空间的稳定性。
        - 将运行时证据映射到SMARt的四个治理状态：
            - `REGULATED`（最高风险）: 触发硬件急停。
            - `ASSISTED`（高风险）: 冻结自主任务，需外部协助。
            - `META-COGNITIVE`（中风险）: 暂停新任务，允许受限的恢复动作（如半速继续当前命令）。
            - `STABLE`（正常）: 允许完整的自治执行。
        - 定义了**会合策略三元组** `Π`：规定了在紧急情况下的降级时机（Drain/Hold）和恢复策略（Return），并提供了形式化验证的条件。

#### 3. 实验设计：数据集、场景与对比方法

- **实验场景**：
    - **仿真环境**：一个由三台UR5机械臂组成的虚拟装配单元，机械臂按预定轨迹协同工作，共享物理工作空间。
    - **故障模型**：模拟一个“主故障Agent”的摄像头出现传感器漂移，该漂移服从Ornstein-Uhlenbeck过程。故障幅度根据NIST的*机器人手臂位置精度退化测量*数据集校准。
    - **事件参数**：90%的模拟为正常校准漂移（故障幅度12mm），10%为严重的多故障级联（故障幅度120mm）。使用10,000个Monte Carlo模拟回合（每轮150个周期）进行评估。

- **Baseline方法**：
    - **单智能体基线**：将单智能体的效用门独立应用于每个Agent。当任一Agent的效用门关闭时，整个系统冻结；当所有门打开时，系统以最高档位（G4）运行。该基线没有SMARt治理状态层（不含τmeta, τcrit, E-Stop）。

- **对比方法**：
    - 通过与Baseline对比，验证EntropyRuntime在**异常检测率**、**检测延迟**和**安全性**方面的提升。
    - 还进行了**组件消融实验**，分别移除“档位”、“效用门”、“回退机制”等组件，以证明每个组件的必要性。

#### 4. 资源与算力

- **文中未明确说明。** 论文指出实验是基于Monte Carlo仿真，使用10,000个回合进行模拟。并没有提及具体的GPU型号、数量或训练时长。这暗示实验可能不依赖大规模GPU集群，而是在CPU或单GPU上进行的仿真计算。

#### 5. 实验数量与充分性

- **实验数量**：
    - **主实验**：10,000个Monte Carlo回合（多智能体场景）。
    - **单智能体消融**：每种条件100个自主运行周期。
    - **多智能体组件消融**：10,000回合。
- **充分性分析**：
    - **充分**：实验设计较为全面，覆盖了单智能体、多智能体正常/故障场景，并进行了详细的消融研究，能够有力支撑主要结论。
    - **有局限**：虽然仿真次数（10,000次）较多，但所有实验基于同一个仿真环境和故障模型，缺乏在真实物理机器人或不同CPS应用（如自动驾驶、无人机编队）上的验证。结论的泛化能力尚需验证。

#### 6. 论文的主要结论与发现

- **单智能体**：形式化证明了EntropyRuntime框架的各项核心安全与稳定性保证（单调稳定、执行安全等），并通过消融实验验证了每个组件（档位、效用门、回退）对于系统安全、稳定和连续运行都是不可或缺的。
- **多智能体**：
    - **性能提升**：在UR5装配单元仿真中，EntropyRuntime相比单智能体Baseline，**异常检测率从2.1%提升至99.6%**（提升47.7倍），**平均检测延迟从43.1个周期降低到12.2个周期**（降低3.5倍）。
    - **安全性保证**：实现了形式化的物理工作空间安全证书（Lyapunov证书），并证明了在所有假设成立的情况下可实现**零碰撞保证**。
    - **核心发现**：传统的单智能体效用门在多智能体场景下**完全无效**，因为它检测不到传感器层的故障。真正的改进来自引入了`τmeta`阈值，用于检测基于风险的信号（`rmax`，一种与时钟速度无关的度量），而不是依赖效用值。
    - **分离关注点**：成功分离了“执行档位”（微权限，控制动作）和“SMARt运行时治理状态”（宏观治理，控制权限授予），使系统行为形式化可分析。

#### 7. 优点：方法或实验设计上的亮点

- **方法创新性**：
    - **“档位”抽象**：提供了一个直观且强大的机制来动态缩放Agent的行动权限，连接了高层的治理策略与底层的动作控制。
    - **形式化保证**：不仅提供了系统的工程实现，还给出了多个强形式化的证明（稳定性、安全性、完备性等），理论价值高。
    - **分离关注点设计**：明确区分了“行动控制”（由EntropyRuntime负责）和“自治治理”（由SMARt状态负责），降低了系统复杂性，并使决策过程透明、可审计。
- **实验设计亮点**：
    - **基于真实数据的故障模型**：使用NIST公开数据集校准故障幅度，增强了仿真的现实性和可信度。
    - **详尽的消融实验**：在单智能体和多智能体场景下都进行了组件消融，清晰地展示了每个组件贡献，有力地支撑了方法的必要性。
    - **对比意义明确**：比较对象是“单智能体方法的直接复用”，这种Baseline选择非常合理，突出了多Agent协同场景带来的独特挑战和该方法的优势。

#### 8. 不足与局限

- **理论假设的理想化**：形式化证明依赖于一些假设，例如**环境平稳性**（定理3）和**故障过程符合OU过程**（定理8）。在非平稳或故障模式更复杂（如阶跃故障、间歇性故障）的真实世界中，这些保证可能不成立。
- **效用函数人工设计**：框架不学习效用函数，其设计依赖于领域专家。选择错误的效用权重或安全阈值`θ`可能导致系统过于保守（生产力下降）或过于危险。
- **“安全”的狭隘定义**：论文中的“安全”主要集中在物理碰撞和动作的“非负效用”上。对于LLM Agent，未考虑输出有偏见、有毒或违反伦理的内容等更复杂的AI安全问题。
- **实验泛化性不足**：
    - **场景单一**：仅在一个三台UR5机械臂的特定装配场景中验证。
    - **Agent数量有限**：未论证其扩展到更大规模（如成百上千个Agent）团队时的可扩展性（共识检查为线性，但风险评估可能是二次增长）。
    - **无真实机器人验证**：所有实验都是仿真，缺乏对真实机器人硬件、传感器噪声和物理动力学不确定性的验证。
- **局限性总结**：论文提出的方法虽然理论扎实、实验效果好，但其适用性受限于其严格的假设、需要人工设计的效用函数以及在单一仿真环境下的验证。

（完）
